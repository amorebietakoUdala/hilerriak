{% extends 'base.html.twig' %}

{% block title %}{{ 'app.title.cemetery.index' | trans }}{% endblock %}

{% block body %}
	<main class="container-fluid">
        <div id="content" class="container-fluid" {{ stimulus_controller('table', 
                    { 'url': path('owner_index'),
                    'exportName': 'owners',
                })}} data-action="entity:success->table#refreshContent" data-table-target="content">
                {{ include('cemetery/_list.html.twig', { 
                    'cemeterys': cemeterys,
                    'pageSize' : pageSize,
                    'page': page,
                    'sortName': sortName,
                    'sortOrder': sortOrder,
                }) }}
        </div>
	</main>
{% endblock %}