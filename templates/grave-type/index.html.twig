{% extends 'base.html.twig' %}

{% block title %}{{ 'app.title.graveTypes.index' | trans }}{% endblock %}

{% block body %}
	<main class="container-fluid">
        <div id="content" class="container-fluid" {{ stimulus_controller('table', 
                    { 'url': path('graveType_index'),
                    'exportName': 'graveTypes',
                })}} data-action="entity:success->table#refreshContent" data-table-target="content">
                {{ include('grave-type/_list.html.twig', { 
                    'graveTypes': graveTypes,
                    'pageSize' : pageSize,
                    'page': page,
                    'sortName': sortName,
                    'sortOrder': sortOrder,
                }) }}
        </div>
	</main>
{% endblock %}