{% extends 'base.html.twig' %}

{% block title %}{{ 'app.title.grave.index' | trans }}{% endblock %}

{% block body %}
	<main class="container-fluid">
        {{ include('grave/_searchForm.html.twig', {
        })}}
        <div id="content" class="container-fluid" {{ stimulus_controller('table', 
                    { 'url': path('grave_index'),
                    'exportName': 'graves',
                })}} data-action="entity:success->table#refreshContent" data-table-target="content">
                {{ include('grave/_list.html.twig', { 
                    'graves': graves,
                    'pageSize' : pageSize,
                    'page': page,
                    'sortName': sortName,
                    'sortOrder': sortOrder,
                }) }}
        </div>
	</main>
{% endblock %}